/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Playlist } from "./Playlist";
import { PlaylistCountArgs } from "./PlaylistCountArgs";
import { PlaylistFindManyArgs } from "./PlaylistFindManyArgs";
import { PlaylistFindUniqueArgs } from "./PlaylistFindUniqueArgs";
import { CreatePlaylistArgs } from "./CreatePlaylistArgs";
import { UpdatePlaylistArgs } from "./UpdatePlaylistArgs";
import { DeletePlaylistArgs } from "./DeletePlaylistArgs";
import { TrackFindManyArgs } from "../../track/base/TrackFindManyArgs";
import { Track } from "../../track/base/Track";
import { SpotifyAccount } from "../../spotifyAccount/base/SpotifyAccount";
import { PlaylistService } from "../playlist.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Playlist)
export class PlaylistResolverBase {
  constructor(
    protected readonly service: PlaylistService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Playlist",
    action: "read",
    possession: "any",
  })
  async _playlistsMeta(
    @graphql.Args() args: PlaylistCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Playlist])
  @nestAccessControl.UseRoles({
    resource: "Playlist",
    action: "read",
    possession: "any",
  })
  async playlists(
    @graphql.Args() args: PlaylistFindManyArgs
  ): Promise<Playlist[]> {
    return this.service.playlists(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Playlist, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Playlist",
    action: "read",
    possession: "own",
  })
  async playlist(
    @graphql.Args() args: PlaylistFindUniqueArgs
  ): Promise<Playlist | null> {
    const result = await this.service.playlist(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Playlist)
  @nestAccessControl.UseRoles({
    resource: "Playlist",
    action: "create",
    possession: "any",
  })
  async createPlaylist(
    @graphql.Args() args: CreatePlaylistArgs
  ): Promise<Playlist> {
    return await this.service.createPlaylist({
      ...args,
      data: {
        ...args.data,

        spotifyAccount: args.data.spotifyAccount
          ? {
              connect: args.data.spotifyAccount,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Playlist)
  @nestAccessControl.UseRoles({
    resource: "Playlist",
    action: "update",
    possession: "any",
  })
  async updatePlaylist(
    @graphql.Args() args: UpdatePlaylistArgs
  ): Promise<Playlist | null> {
    try {
      return await this.service.updatePlaylist({
        ...args,
        data: {
          ...args.data,

          spotifyAccount: args.data.spotifyAccount
            ? {
                connect: args.data.spotifyAccount,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Playlist)
  @nestAccessControl.UseRoles({
    resource: "Playlist",
    action: "delete",
    possession: "any",
  })
  async deletePlaylist(
    @graphql.Args() args: DeletePlaylistArgs
  ): Promise<Playlist | null> {
    try {
      return await this.service.deletePlaylist(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [Track], { name: "tracks" })
  @nestAccessControl.UseRoles({
    resource: "Track",
    action: "read",
    possession: "any",
  })
  async findTracks(
    @graphql.Parent() parent: Playlist,
    @graphql.Args() args: TrackFindManyArgs
  ): Promise<Track[]> {
    const results = await this.service.findTracks(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => SpotifyAccount, {
    nullable: true,
    name: "spotifyAccount",
  })
  @nestAccessControl.UseRoles({
    resource: "SpotifyAccount",
    action: "read",
    possession: "any",
  })
  async getSpotifyAccount(
    @graphql.Parent() parent: Playlist
  ): Promise<SpotifyAccount | null> {
    const result = await this.service.getSpotifyAccount(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
